language: java
jdk:
  - openjdk8
branches:
  only:
    - main
cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.gradle"
script: "./gradlew clean build -x test"

notifications:
  email:
    recipients:
      - cgc8016@ewhain.net